

GraphQL을 사용하는 경우 gql, hooks로 나눌 수 있다.

export const FETCH_BOARDS = gql`` 이렇게 API를 지정해 불러올 데이터는 api 폴더에 생성을하면 된다.

export const useQueryBoards = () => {} 이렇게 hooks 생성은 hooks 폴더에 생성을 하면된다.

같이 사용하는 경우 hooks에 같이 만들면 되는데 취향것 할 것